const getters = {
  // sidebar: state => state.app.sidebar,

  // 各种 Select 选择器 的各种选项集合
  // $store.getters.shopGroupOptions
  // cardTypeOptions: state => state.selectOptions.cardTypeOptions,
  // goodsList: state => state.selectOptions.goodsList,
}
export default getters
